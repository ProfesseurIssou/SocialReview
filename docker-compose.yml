version: '3.3'

      
services:
    SocialReview:
        image: ghcr.io/professeurissou/socialreview:main
        container_name: SocialReview
        environment:
            - TZ=Europe/Paris
        ports:
            - 5001:5001
        restart: unless-stopped